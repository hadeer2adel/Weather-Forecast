<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherForecast" parent="Theme.Material3.DayNight">
        <!-- Disable the default action bar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="switchStyle">@style/Widget.App.Switch</item>
        <item name="radioButtonStyle">@style/Widget.App.RadioButton</item>
        <item name="android:popupMenuStyle">@style/PopupMenuStyle</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Widget.App.Switch" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Color</item>
    </style>

    <style name="Widget.App.RadioButton" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Color</item>
    </style>

    <style name="ThemeOverlay.App.Color" parent="">
        <item name="colorOnSurface">@color/dark_color_4</item>
        <item name="colorSecondary">@color/light_color_4</item>
    </style>

    <style name="PopupMenuStyle" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@drawable/bg_color_3</item>
        <item name="android:textColor">@color/dark_color_1</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="Theme.WeatherForecast" parent="Base.Theme.WeatherForecast" />
</resources>
